import 'package:flutter/material.dart';

class Mapuse extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return new Scaffold(
      appBar: new AppBar(
        backgroundColor: Colors.red[900],
        title: new Text('Please Dont answer the following questions')
      ),
    );
  }
}